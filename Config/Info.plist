<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- User-facing app name (the Xcode target stays "bigcsv"). -->
	<key>CFBundleDisplayName</key>
	<string>BigCSV</string>
	<!-- App Store category (required for Mac App Store submission). -->
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.productivity</string>
	<!-- No encryption beyond what's exempt → pre-answer export compliance so future
	     uploads don't ask the encryption question. BigCSV has no crypto and no network. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Declare the document types we can open so Finder "Open With" / double-click work.
	     We use system-declared UTIs (no custom UTI needed) and rank Alternate so we don't
	     steal CSV ownership from Numbers/Excel. Role is Viewer: we are strictly read-only. -->
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>Delimited Text</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>LSHandlerRank</key>
			<string>Alternate</string>
			<key>LSItemContentTypes</key>
			<array>
				<string>public.comma-separated-values-text</string>
				<string>public.tab-separated-values-text</string>
				<string>public.plain-text</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
